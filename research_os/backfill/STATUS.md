# W-B backfill — resume note (checkpoint 2026-06-01)

> **✅ W-B COMPLETE 2026-06-01.** All three steps below are done. The store is
> referentially closed (`python research_os/backfill/reconcile.py` exits 0). See
> `research_os/backfill/README.md` for the final coverage + lossiness notes. The
> sections below are kept as the historical recipe. Not yet committed.

Phase 0 schemas are DONE and approved. W-B (lossy-pragmatic backfill of 146 experiments +
86 memory files + PROGRESS into run records / claim cards / goal tree) is **in progress**.
This note lets a cold next-window agent resume in minutes. Read `research_os/PLAN.md` §6/§12
+ the memory card `project_research_os.md` first if you lack context.

## DONE
- **Phase 0 schemas** (`research_os/schemas/`): 6 schemas v1.0.0, README, 10 examples, validators.
  Both flagged deviations (substrate_component 6th schema; claim status += draft/retracted)
  were **APPROVED** by the user. Run-record `id` pattern was relaxed to accept full filename
  stems (e.g. `s059i_validator_perturbed`). All schemas meta-valid; examples 10/10 valid.
- **Goal tree** (`research_os/goals/`): 35 nodes (1 thesis + 8 chapters + 26 non-empty leaves),
  all schema-valid, generated by `research_os/backfill/build_goal_tree.py`. `_index.json` is derived.
- **Experiment→node classification**: all 146 experiments mapped (the `EXPLICIT`/`OVERRIDES`
  tables in build_goal_tree.py). Coverage gate passes (146/146).
- **Run manifest** (`research_os/backfill/run_manifest.json`): 146 entries, frontmatter parsed
  (145/146 clean; s112 has no frontmatter → subagent reads its body).
- **Extraction contract** (`research_os/backfill/EXTRACT_INSTRUCTIONS.md`) + `schemas/validate_one.py`.
- **Run records: 8 / 146 written** (`research_os/records/`) — the PILOT batch, all valid:
  s001, s019, s043, s058, s082, s097, s106, s108 (+their stems). Pilot validated the approach;
  judgment calls (oracle_clean, status, propagator era) are sound. NOTE: all 8 pilot records are
  oracle_clean=false (correct — they all read truth; later genuinely-blind experiments like
  s099/s100 should come out oracle_clean=true).

## TODO (in order)

### 1. Finish run-record extraction — 138 remaining (task #4)
Idempotent: every extraction agent SKIPS ids whose `research_os/records/<id>.json` already exists,
so the 8 pilot records are safe. Launch ~7 `general-purpose` subagents (Agent tool), each with a
short prompt: "Read research_os/backfill/EXTRACT_INSTRUCTIONS.md + research_os/schemas/run_record.schema.json
in full, then process these ids: [...]. Work from repo root. Write records, validate, return the
compact JSON array + WROTE/SKIPPED line." Split the 146 manifest ids into 7 contiguous batches
(~21 each) — the skip-existing rule handles the pilot overlap. Run in BACKGROUND if context is tight;
reconcile from the on-disk files (step 3), NOT from the agents' return messages.
After: `for f in research_os/records/*.json; do python research_os/schemas/validate_one.py "$f" run_record; done`
should show 146 valid.

### 2. Spine claim cards (task #2) — NOT STARTED
Write to `research_os/claims/research/` and `research_os/claims/preferences/`. The high-value spine
(I have it from MEMORY.md + PROGRESS): the **supersede chains** (corrections-as-pointers) —
 - anchor-baseline-aliasing-is-the-blocker → superseded_by hard-shoot-trap-is-the-blocker (s099→s100/s105);
 - full-LC-RMSE-fails (pre times0 fix) → superseded_by full-LC-RMSE-works (s093 reversed by s097);
 - cloud-data PROVEN-on-89 → **retracted** (oracle injection, s058/s059);
 - propagator helping m103 / pre-fix findings → needs_replication via propagator@1.0.0 blast-radius.
Plus load-bearing live claims (pol_diam↔basin -0.94; body-twin halving; LC-only ω priors dead;
windowed-photometry breaks hard-shoot-trap; s019 bracket held-out 20/20) and the preference deck
(|ω| in deg/s; surrogate-first hi-fi-last; save-intermediate; no-parallel-CPU; physical |ω| bracket).
The 4 example claim cards in `schemas/examples/` are templates. Best done by ME (synthesis), or scripted
like build_goal_tree.py. The example claims reference SHORT run ids (s106) — real claims must reference
the full stems (s106_hybrid_loss_polish) to satisfy referential integrity.

### 3. Reconcile + validate + manifest (task #5)
Write `research_os/backfill/reconcile.py` to: (a) validate every object against its schema;
(b) fill each `goal_node.child_runs` from the records that point at it (rewrite the node files);
(c) fill claim `supersedes` back-edges + verify supporting/refuting_runs resolve; (d) run the
referential-integrity checks listed in `schemas/README.md`; (e) write `research_os/backfill/README.md`
(coverage, what's lossy, how to regenerate). Then update the memory card to "W-B complete".

## Commit
Nothing has been committed — the user has not asked. All work is durable on disk. Suggest committing
the `research_os/` tree on branch `forward_survey` once W-B is further along (ask the user first).
