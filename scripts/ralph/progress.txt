# Ralph Progress Log
Started: Fri Feb  6 05:19:30 PM NZDT 2026
---

## Codebase Patterns
- Notebooks use Jupytext percent format with `formats: ipynb,py:percent` header
- Setup pattern: PROJECT_ROOT detection, sys.path insert, os.chdir, then imports
- ObjectiveFunction takes `compute_shadows_flag` (True=hi-fi with ray tracing, False=lo-fi with all-lit approximation)
- True attitude params stored as 6-element vector: [axis_angle_x, axis_angle_y, axis_angle_z, omega_x, omega_y, omega_z]
- CountedObjective wrapper normalizes axis-angle via quaternion round-trip before evaluation
- Parameter bounds: axis-angle [-pi, pi], omega [-30 deg/s, 30 deg/s] in radians
- Success criteria: omega error < 0.1 deg/s AND RMS residual < 2x noise_sigma
- SPICE Observer ID for ground station: 399999
- Fixed articulation: solar panels 0 deg, antenna dishes 15 deg
- Inertia tensor computed with specific component masses (Bus: 1532, SP: 170 each, AD: 50 each)

---

## 2026-02-06 - US-001
- Created notebooks/inversion/08_mixed_fidelity_inversion.py as Jupytext percent format notebook
- Implemented full Intelsat 901 setup (config, STL loading, BRDF, SPICE, articulation, inertia tensor)
- Generated synthetic lightcurve with tumbling dynamics and shadows enabled (n_obs=50, noise_sigma=0.05)
- Created dual-fidelity objectives: obj_hifi (shadows ON) and obj_lofi (shadows OFF)
- Both objectives verified at true_params
- Stored true_params, param_names, bounds matching notebook 06 format
- Included evaluate_success() and CountedObjective wrapper from notebook 06
- Added setup verification section
- Files changed: notebooks/inversion/08_mixed_fidelity_inversion.py (new)
- **Learnings for future iterations:**
  - The setup pattern is identical across notebooks 04-07; copy sections 1-6 from notebook 06
  - ObjectiveFunction's compute_shadows_flag controls whether ray tracing is used; False uses create_no_shadow_lit_status() for all-lit approximation
  - Need to use venv Python for jupytext validation: `source .venv/bin/activate`
  - No formal typechecker (mypy/pyright) is installed; use py_compile and AST parsing for syntax checks
  - Branch was created from ralph/lightcurve-inversion (not master) since that's where notebooks 04-07 live
---

## 2026-02-06 - US-002
- Implemented Experiment 1a: Evaluation timing benchmark (N=20 evals of each fidelity level)
- Prints mean/std timing for hi-fi and lo-fi, plus speedup factor
- Implemented Experiment 1b: Lightcurve comparison at true parameters
- Extracts predicted magnitudes from both objectives using _generate_predicted_lightcurve()
- Computes magnitude residual (hifi - lofi) with mean, std, range, and RMS statistics
- Files changed: notebooks/inversion/08_mixed_fidelity_inversion.py (appended cells)
- **Learnings for future iterations:**
  - To extract predicted lightcurves, replicate evaluate() pipeline: axis_angle_to_quaternion → propagate_attitude → _compute_body_frame_vectors → _generate_predicted_lightcurve
  - Body-frame vectors (k1, k2) are identical for both objectives since geometry/attitude are the same; only the shadow computation differs in _generate_predicted_lightcurve
  - ObjectiveFunction._compute_chi_squared returns MSE (sum(residuals^2)/n_valid) when no uncertainties are provided
---

## 2026-02-06 - US-003
- Implemented Experiment 1c: Rank correlation (Spearman) between lo-fi and hi-fi objectives
- Evaluates 100 random parameter samples on both objectives within bounds
- Computes Spearman rank correlation using scipy.stats.spearmanr
- Created 3-panel benchmark figure (mixed_fidelity_benchmark.png):
  - (a) Lightcurve overlay (hi-fi vs lo-fi at true params)
  - (b) Magnitude residual vs time with noise_sigma band
  - (c) Scatter of lo-fi vs hi-fi objective values with Spearman rho annotation
- Added markdown summary cell with expected range assessment
- Files changed: notebooks/inversion/08_mixed_fidelity_inversion.py (appended cells)
- **Learnings for future iterations:**
  - scipy.stats.spearmanr returns (rho, p_value) tuple
  - Use np.random.uniform(lower, upper, size=(n, dim)) for batch random sampling within bounds
  - Figure saving pattern: PROJECT_ROOT / "data" / "results" / "inversion_diagnostics" / "filename.png" at 150 DPI with bbox_inches='tight'
  - Jupytext may update its version number in the header during sync (1.19.0 → 1.19.1) - this is normal
---

## 2026-02-06 - US-004
- Implemented Experiment 2a: Basin shift from true parameters
  - Runs L-BFGS-B from true_params on both obj_hifi and obj_lofi
  - Compares converged solutions: Euclidean distance, axis-angle displacement (degrees), omega displacement (deg/s)
- Implemented Experiment 2b: Perturbed starts basin shift analysis
  - Defined run_local_optimization() wrapper with axis-angle quaternion normalization
  - Reused sample_in_ball() pattern from notebook 05 with param_scales (1 degree units)
  - Runs 20 perturbed starts within 2-degree basin radius on both fidelity levels
  - Computes pairwise displacement statistics: mean, std, max
  - Qualitative assessment: negligible (<1% basin width), moderate (1-10%), significant (>10%)
- Added markdown summary cell interpreting basin shift results
- Files changed: notebooks/inversion/08_mixed_fidelity_inversion.py (appended cells)
- **Learnings for future iterations:**
  - run_local_optimization() must be defined locally in notebook 08 (not imported from notebook 05)
  - sample_in_ball() uses param_scales to measure distance in degrees across heterogeneous parameter types
  - Basin radius of 2 degrees is a reasonable moderate radius for perturbed start analysis
  - Qualitative assessment thresholds: <1% = negligible, 1-10% = moderate, >10% = significant shift relative to basin width
---
