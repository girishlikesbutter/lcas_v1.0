# Ralph Progress Log
Started: Thu Feb 06 2026

## Codebase Patterns
- Project uses requirements.txt for dependencies (not pyproject.toml)
- No mypy/pyright configured - use `python -m compileall src/ -q` for syntax validation
- Python source files are in src/ directory with standard module structure
- Jupytext notebooks use percent format (.py files paired with .ipynb)
- Inversion module is in src/inversion/ with ObjectiveFunction, optimizers, and utilities
- Dynamics module is in src/dynamics/ with propagate_attitude() for attitude propagation
- Existing notebooks 01-03 in notebooks/inversion/ demonstrate inertia, propagation, and basic inversion
- Use Intelsat 901 config from data/models/intelsat_901/ for testing
- SPICE metakernel at data/spice_kernels/lcas_meta_generic_v2.tm

---

## 2026-02-06 08:07 - US-001
- Created data/results/inversion_diagnostics/ directory
- Added .gitkeep file to preserve directory in git
- Updated .gitignore to allow tracking inversion_diagnostics/ directory (added negation rules)
- Verified typecheck passes with `python -m compileall src/ -q`
- Files changed: .gitignore, data/results/inversion_diagnostics/.gitkeep (new)
- **Learnings for future iterations:**
  - .gitignore has pattern `data/results/*/` that ignores all result subdirectories
  - Need to add negation rules `!data/results/<dirname>/` to allow tracking new result directories
---

## 2026-02-06 08:15 - US-002
- Created notebooks/inversion/04_landscape_analysis.py as Jupytext percent format notebook
- Reused Intelsat 901 configuration loading from notebook 03
- Computed observation geometry using same parameters (n_observations=50, same time window)
- Generated synthetic lightcurve using forward model with tumbling mode and shadows enabled
- Created true_params = np.concatenate([true_axis_angle, true_omega0]) for slicing operations
- Created ObjectiveFunction instance configured for landscape analysis
- Verified objective value at true parameters works correctly
- Typecheck passed with `python -m compileall src/ notebooks/inversion/04_landscape_analysis.py -q`
- Files changed: notebooks/inversion/04_landscape_analysis.py (new, 398 lines)
- **Learnings for future iterations:**
  - ObjectiveFunction takes mode="tumbling" and inertia_tensor parameters for proper physics
  - true_params vector format is [axis_angle_x, axis_angle_y, axis_angle_z, omega_x, omega_y, omega_z]
  - axis_angle is in radians, omega is in rad/s
  - Use quaternion_to_axis_angle() to convert q0 to axis-angle representation
---

## 2026-02-06 - US-003
- Implemented compute_1d_slice() function for computing 1D objective function slices
- Computed slices for all 6 parameters with appropriate delta ranges:
  - axis_angle (x,y,z): +/-0.5 rad
  - omega (x,y,z): +/-0.01 rad/s
- Created 2x3 subplot figure with linear scale visualization
- Added log y-axis version to see structure near minimum
- Marked true parameter location (delta=0) on each plot
- Added quantitative analysis summary (min objective, curvature at min, objective range)
- Saves figures to data/results/inversion_diagnostics/1d_slices.png and 1d_slices_linear.png
- Files changed: notebooks/inversion/04_landscape_analysis.py (+205 lines)
- **Learnings for future iterations:**
  - Use time.time() for simple timing of expensive computations
  - slice_results dict stores (deltas, objectives) tuples keyed by parameter name
  - Curvature approximation uses central difference: f''(x) ≈ (f(x+h) - 2f(x) + f(x-h)) / h^2
  - semilogy() for log-scale y-axis visualization
---

## 2026-02-06 - US-004
- Implemented compute_2d_slice() function for computing 2D objective function slices
- Computed 2D slices for 4 parameter pairs:
  - (axis_angle_x, axis_angle_y): orientation coupling
  - (axis_angle_x, omega_z): orientation-spin coupling
  - (omega_x, omega_y): transverse angular velocity coupling
  - (omega_x, omega_z): transverse-spin angular velocity coupling
- Created 2x2 subplot figure with heatmaps and contour overlays
- Marked true parameter location (red star) on each heatmap
- Added quantitative analysis summary (global min, objective range)
- Added markdown cell explaining interpretation of 2D slices
- Saves figure to data/results/inversion_diagnostics/2d_slices.png
- Files changed: notebooks/inversion/04_landscape_analysis.py (+204 lines)
- **Learnings for future iterations:**
  - 2D slice indexing: objectives[j, i] where j is y-axis, i is x-axis
  - Use pcolormesh with shading='auto' for heatmaps, then overlay contour lines
  - Store param indices with results tuple for proper label lookup
  - np.unravel_index() to convert flat argmin to 2D indices
---

## 2026-02-06 - US-005
- Implemented compute_numerical_gradient() function using central finite differences
- Implemented compute_numerical_hessian() function using finite differences
- Added Hessian eigenvalue analysis with:
  - Eigenvalue sorting by magnitude
  - Condition number calculation (|λ_max| / |λ_min|)
  - Positive definiteness check
- Added markdown table for condition number interpretation (>1000 = ill-conditioned)
- Added parameter constraint classification:
  - Eigenvector component analysis to identify poorly constrained directions
  - Simplified classification using Hessian diagonal elements
- Files changed: notebooks/inversion/04_landscape_analysis.py (+291 lines)
- **Learnings for future iterations:**
  - Use different step sizes for gradient (smaller ~1e-5 for axis_angle, ~1e-7 for omega)
  - Use slightly larger step sizes for Hessian (~1e-4 for axis_angle, ~1e-6 for omega)
  - np.linalg.eigh() returns eigenvalues sorted ascending, need to sort by magnitude for analysis
  - Central difference: f'(x) ≈ (f(x+h) - f(x-h)) / (2h)
  - Mixed partial: f''_ij ≈ (f(++)-f(+-)-f(-+)+f(--)) / (4h_i*h_j)
---

## 2026-02-06 - US-006
- Added landscape analysis summary section (Section 11) to notebook 04
- Created markdown cells summarizing key findings:
  - Minimum uniqueness assessment based on 1D slice analysis
  - Minimum sharpness interpretation from Hessian condition number
  - Flat directions identification from eigenvalue analysis
  - Global vs local optimization recommendation based on landscape characteristics
- Implemented code to generate comprehensive landscape_summary.txt report with:
  - 5 sections: Minimum Uniqueness, Sharpness, Flat Directions, Optimization Strategy, Key Metrics
  - Dynamic assessment based on actual computed values (condition number, eigenvalues, etc.)
  - Clear recommendations for optimization approach
- Summary saved to data/results/inversion_diagnostics/landscape_summary.txt
- Added conclusions markdown cell summarizing what the notebook accomplishes
- Typecheck passed with `python -m compileall src/ notebooks/inversion/04_landscape_analysis.py -q`
- Files changed: notebooks/inversion/04_landscape_analysis.py (+264 lines)
- **Learnings for future iterations:**
  - Summary reports should be built programmatically from computed values, not hardcoded
  - Use clear thresholds for classification (e.g., condition number < 100 for "well-conditioned")
  - Parameter constraint classification uses Hessian diagonal relative to median curvature
  - Minimum uniqueness can be checked by comparing argmin to center index in 1D slices
---

## 2026-02-06 - US-007
- Created notebooks/inversion/05_basin_size_study.py as Jupytext percent format notebook
- Copied minimal setup from notebook 04 (Intelsat 901 config, geometry, synthetic lightcurve)
- Implemented run_local_optimization(objective_fn, x0, bounds) function:
  - Uses scipy L-BFGS-B with ftol=1e-8, gtol=1e-6
  - Normalizes axis-angle via quaternion round-trip during optimization
  - Returns (x_opt, f_opt, success, n_evals) tuple
- Defined success criteria with evaluate_success() function:
  - Angular velocity error < 0.1 deg/s
  - RMS residual < 2 x noise_sigma
- Added test from true parameters to verify optimization works
- Typecheck passed with `python -m compileall src/ notebooks/inversion/05_basin_size_study.py -q`
- Files changed: notebooks/inversion/05_basin_size_study.py (new, 617 lines)
- **Learnings for future iterations:**
  - Always normalize axis-angle via quaternion round-trip during optimization (axis_angle_to_quaternion -> normalize_quaternion -> quaternion_to_axis_angle)
  - L-BFGS-B bounds format: list of (min, max) tuples
  - RMS from objective: sqrt(objective_value / n_observations) since objective is sum of squared residuals
  - Success criteria should check BOTH parameter error AND residual quality
---

## 2026-02-06 - US-008
- Implemented sample_in_ball() function for uniform sampling within hypersphere
  - Uses Gaussian direction normalization for uniform direction on unit sphere
  - Uses r = R * u^(1/n) for uniform density in ball (proper CDF inversion)
  - param_scales handle different units (axis-angle in rad, omega in rad/s)
- Implemented verify_samples_in_ball() function to check samples are within radius
- Defined param_scales using 1 degree as natural unit for both orientation and angular velocity
  - AXIS_ANGLE_SCALE = np.deg2rad(1.0)
  - OMEGA_SCALE = np.deg2rad(1.0)
- Added test section (Section 12) with visualization
  - Histogram of scaled distances
  - Per-parameter offset distribution
  - Saves test plot to data/results/inversion_diagnostics/sample_in_ball_test.png
- Typecheck passed with `python -m compileall src/ notebooks/inversion/05_basin_size_study.py -q`
- Files changed: notebooks/inversion/05_basin_size_study.py (+219 lines)
- **Learnings for future iterations:**
  - For uniform sampling in n-dimensional ball: r = R * u^(1/n) where u ~ Uniform(0,1)
  - Expected mean distance from center in n-D ball is n/(n+1) * R
  - Use param_scales to normalize different parameter types for proper distance measurement
  - verify_samples_in_ball() is useful for validating sampling correctness
---

## 2026-02-06 - US-009
- Implemented basin size sweep experiment (Section 13) in notebook 05
- Configuration:
  - Test radii: [0.01, 0.05, 0.1, 0.2, 0.5, 1.0, 2.0, 5.0, 10.0] degrees
  - 20 trials per radius = 180 total local optimizations
  - Uses sample_in_ball() with different seeds per radius for diversity
- Data structures created:
  - sweep_results: Dict with per-trial data (success, omega_error, rms_residual, n_evals, final_objective)
  - summary_stats: Dict with aggregated statistics per radius (success_rate, mean/std of errors and evals)
- Tracks: success rate, mean final omega error, mean RMS residual, mean function evaluations
- Prints progress and summary table during execution
- Typecheck passed with `python -m compileall src/ notebooks/inversion/05_basin_size_study.py -q`
- Files changed: notebooks/inversion/05_basin_size_study.py (+155 lines)
- **Learnings for future iterations:**
  - Use base_seed = 1000 * (r_idx + 1) to get different random samples for each radius
  - sweep_results dict stores 2D arrays (n_radii x n_trials) for efficient analysis
  - summary_stats dict stores 1D arrays for easy plotting
  - Print progress during long sweeps to show activity
---

## 2026-02-06 - US-010
- Implemented basin size visualization and threshold identification (Section 14-15) in notebook 05
- Added compute_binomial_error() function for proper uncertainty estimation of success rates
- Created visualization plots:
  - Success rate vs radius with error bars and 80% threshold line
  - Mean omega error vs radius with std deviation error bars
  - Function evaluations vs radius
  - RMS residual vs radius with noise sigma reference lines
- Implemented find_critical_radius_80() to identify critical radius via log-space interpolation
- Added comprehensive summary findings section with:
  - Critical radius identification and interpretation
  - Success rate profile categorization (high/moderate/low)
  - Optimization cost analysis
  - Practical implications based on basin size
- Saves figures to: basin_size_success_rate.png, basin_size_metrics.png
- Saves text summary to: basin_size_summary.txt
- Added conclusions markdown cell explaining notebook outputs
- Typecheck passed with `python -m compileall src/ notebooks/inversion/05_basin_size_study.py -q`
- Files changed: notebooks/inversion/05_basin_size_study.py (+415 lines)
- **Learnings for future iterations:**
  - Binomial uncertainty: sqrt(p*(1-p)/n) for standard error
  - Use Wilson score interval approximation for edge cases (p=0 or p=1)
  - Log-space interpolation is better for finding thresholds with exponential radii spacing
  - Categorize basin size as NARROW (<1 deg), MODERATE (1-5 deg), or WIDE (>5 deg)
  - Summary reports should include actionable recommendations based on findings
---

## 2026-02-06 - US-011
- Created notebooks/inversion/06_optimizer_comparison.py as Jupytext percent format notebook
- Copied minimal setup from notebook 04 (Intelsat 901 config, geometry, synthetic lightcurve)
- Implemented CountedObjective class with:
  - __call__() method that counts evaluations and enforces budget limit
  - Returns penalty_value (1e10) when budget is exhausted
  - Tracks best_value and best_params across all evaluations
  - Normalizes axis-angle via quaternion round-trip
- Implemented reset() method to clear n_evals, best_value, best_params for multiple trials
- Added helper methods: get_remaining_budget(), is_budget_exhausted()
- Added test section (Section 9) to verify wrapper behavior
- Reused parameter bounds and evaluate_success() from notebook 05
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (new, 560 lines)
- **Learnings for future iterations:**
  - CountedObjective wrapper pattern: wrap objective_fn, count in __call__, return penalty when exhausted
  - Use float('inf') for initial best_value to ensure any valid evaluation improves it
  - Reset method is essential for running multiple independent trials with same wrapper
  - Budget enforcement returns penalty early (before evaluation) to not waste compute
---

## 2026-02-06 - US-012
- Implemented multistart_local() function in notebook 06 (Section 11)
- Uses scipy.stats.qmc.LatinHypercube for well-distributed initialization points
- LHS provides better parameter space coverage than random sampling
- Function signature: multistart_local(counted_objective, bounds, n_starts, max_evals_per_start, seed)
- Runs L-BFGS-B from each LHS-sampled starting point with ftol=1e-8, gtol=1e-6
- Tracks total evaluations via CountedObjective wrapper to respect budget
- Returns dict with x_best, f_best, n_evals, n_successful_starts, all_results
- Added test section (Section 12) demonstrating multi-start with 5 starts, 500 budget
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (+178 lines)
- **Learnings for future iterations:**
  - scipy.stats.qmc.LatinHypercube(d=n_params, seed=seed).random(n=n_starts) generates samples in [0,1]^n
  - Scale LHS samples: initial_points = lower_bounds + samples_unit * (upper_bounds - lower_bounds)
  - Track evals_before = counted_objective.n_evals before each local optimization to measure per-start cost
  - n_successful_starts counts starts that completed without exhausting total budget
---

## 2026-02-06 - US-013
- Implemented run_de() function for Differential Evolution baseline in notebook 06 (Section 13)
- Matches settings from src/inversion/optimizers.py:
  - strategy='best1bin'
  - mutation=(0.5, 1.0)
  - recombination=0.7
  - updating='deferred'
  - tol=0.01
- Calculates maxiter from budget: maxiter = max_evals / (popsize * n_params) - 1
- Uses scipy default popsize=15 for consistent evaluation count estimation
- Polish disabled to stay within evaluation budget
- Uses CountedObjective.best_params tracking since budget exhaustion returns penalty values
- Added test section (Section 14) demonstrating DE with 500 evaluation budget
- Updated summary section to include run_de() in available objects
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (+119 lines)
- **Learnings for future iterations:**
  - DE evaluations per generation ≈ popsize * n_params (for scipy.optimize.differential_evolution)
  - scipy DE default popsize is 15 (multiplied by n_params internally)
  - When budget exhausts mid-optimization, CountedObjective tracks best seen, which may differ from DE's final result
  - Always disable polish=False when enforcing strict evaluation budgets
---

## 2026-02-06 - US-014
- Implemented run_basinhopping() function in notebook 06 (Section 15)
- Configured scipy.optimize.basinhopping with evaluation budget enforcement:
  - Uses L-BFGS-B as local minimizer with bounds
  - Estimates niter from budget: niter = max_evals / 75 - 2 (conservative)
  - Local minimizer limited to min(200, max_evals // 5) evals per hop
- Set reasonable stepsize and temperature parameters:
  - stepsize = 0.5 rad (reasonable for axis-angle exploration)
  - T = 1.0 (moderate temperature for uphill acceptance)
- Added callback function to stop early when budget exhausted
- Uses CountedObjective.best_params tracking for accurate best result
- Added test section (Section 16) demonstrating basin-hopping with 500 evaluation budget
- Updated summary section to include run_basinhopping() in available objects
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (+186 lines)
- **Learnings for future iterations:**
  - Basin-hopping combines random perturbations with local minimization (hybrid approach)
  - Use callback function returning True to stop basin-hopping early
  - basinhopping result.message can be a list, need to extract first element
  - Typical L-BFGS-B convergence takes 50-100 evals per local optimization
  - stepsize controls how far basin-hopping "hops" between local searches
---

## 2026-02-06 - US-015
- Implemented optimizer comparison experiment in notebook 06 (Section 17)
- Configuration:
  - Fixed budget: 5000 function evaluations for fair comparison
  - 10 trials per strategy with different random seeds (BASE_SEED + trial * 100)
  - Multi-start: 50 starts with ~100 evals per start
- Implemented run_trial(strategy, seed) function that:
  - Creates fresh CountedObjective for each trial
  - Dispatches to multistart_local(), run_de(), or run_basinhopping()
  - Records best objective, omega error, RMS residual, success, wall time
- Results stored in two formats:
  - comparison_results: List of all 30 trial dictionaries
  - strategy_results: Dict with per-strategy numpy arrays for analysis
- Added summary statistics and detailed comparison table output
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (+208 lines)
- **Learnings for future iterations:**
  - Use BASE_SEED + trial * 100 spacing for diverse random sequences per trial
  - Create fresh CountedObjective per trial (not per strategy) for accurate budget tracking
  - Store both raw results (list of dicts) and processed arrays (dict of arrays) for flexibility
  - run_trial() pattern centralizes strategy dispatch and metric collection
---

## 2026-02-06 - US-016
- Implemented optimizer comparison visualization and conclusions in notebook 06 (Sections 18-19)
- Created three visualization plots:
  - Box plot of final objective values by strategy (with jittered individual points)
  - Box plot of parameter errors (omega error in deg/s) with log scale option
  - Bar chart of success rates with 95% Wilson score confidence intervals
- Added compute_binomial_ci() function using Wilson score interval for proper CI calculation
- Created formatted summary table with Strategy | Success Rate | Mean Error | Mean Time | Median Obj
- Implemented dynamic conclusions section that:
  - Identifies best strategy based on success rate
  - Calculates speedup relative to DE baseline
  - Provides strategy-specific recommendations based on results
- Saves all outputs to data/results/inversion_diagnostics/:
  - optimizer_comparison_objectives.png
  - optimizer_comparison_errors.png
  - optimizer_comparison_success_rate.png
  - optimizer_comparison_summary.txt
- Added comprehensive Summary markdown cell documenting notebook outputs and available objects
- Typecheck passed with `python -m compileall src/ notebooks/inversion/06_optimizer_comparison.py -q`
- Files changed: notebooks/inversion/06_optimizer_comparison.py (+413 lines)
- **Learnings for future iterations:**
  - Wilson score interval is better than simple binomial CI for small sample sizes
  - Use patch_artist=True for boxplot to enable coloring boxes
  - Jitter x positions with np.random.normal(i+1, 0.04, len(data)) for scatter overlay on boxplots
  - Set y-axis log scale conditionally based on data range (max/min > 100)
  - Dynamic conclusions should reference computed metrics, not hardcoded values
---

## 2026-02-06 - US-017
- Created notebooks/inversion/07_robustness_analysis.py as Jupytext percent format notebook
- Copied minimal setup from notebook 04 (Intelsat 901 config, geometry, inertia tensor)
- Computed full observation geometry and lightcurve for max_n_observations=100
- Implemented generate_test_case(n_observations, noise_sigma, seed) function:
  - Subsamples from pre-computed full observation set for efficiency
  - Adds Gaussian noise with specified sigma
  - Returns dict with lightcurve, geometry, and configured ObjectiveFunction
  - Uses same true_params across all test cases for comparability
- Added test case verification section with 3 different configurations
- Added visualization section showing effect of noise and observation density
- Reused evaluate_success() function from notebook 06 for consistent metrics
- Saves test_case_examples.png to data/results/inversion_diagnostics/
- Typecheck passed with `python -m compileall src/ notebooks/inversion/07_robustness_analysis.py -q`
- Files changed: notebooks/inversion/07_robustness_analysis.py (new, 650 lines)
- **Learnings for future iterations:**
  - Pre-compute full geometry/lightcurve at max observations, then subsample for efficiency
  - Use np.linspace(...).astype(int) for uniformly spaced subsampling indices
  - Subsample articulation_matrices dict by iterating over items and indexing each array
  - generate_test_case pattern: returns dict with all components needed for optimization
---

